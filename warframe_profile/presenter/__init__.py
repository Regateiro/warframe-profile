"""Presenter layer — orchestrates model logic and view rendering.

Each module corresponds to one CLI sub-command:

    cli.py             — --ducats / --relics
    craft_tree.py      — --craft
    inventory_cleanup.py — --cleanup
    mastery.py         — --mastery
    web_server.py      — --serve
"""
