"""Web view — HTML template rendering for the ``--serve`` sub-command.

Each ``render_*`` function takes model dataclasses and returns an HTML
string wrapped in the shared dark-theme page layout.
"""
