"""View layer — rendering model data as human-readable output.

Sub-packages:
    report.py   — Terminal output via ``prettytable`` and ANSI colours.
    web/        — HTML templates for the web UI (--serve mode).
"""
